# Specs Index

One row per spec folder. Add a row whenever you create a new folder.

| Folder | Topic | Status |
| ------ | ----- | ------ |
| [`01-initial-build`](01-initial-build/01-waves.md) | The 13-wave build plan: goals, deliverables, acceptance criteria, dependencies | ✅ Built (waves 1-13) |
| [`02-test-hardening`](02-test-hardening/05-results.md) | Autonomous test campaign: Playwright E2E, Vitest units, backend gaps, contract drift. **Results in `05-results.md`.** | ✅ Executed - 666 tests, 145/149 rows, 5 bugs fixed |
| [`03-redesign`](03-redesign/01-design-brief.md) | Full visual redesign brief for a design tool: product context, data reality, every page mapped, component inventory, prototype deliverables. **Start at `01-design-brief.md`.** | ✅ Handed off, designed, and built - see `07-visual-redesign` |
| [`04-channel-ingestion`](04-channel-ingestion/01-comedyclubpodcast-run.md) | Per-channel backfill runs and their findings. **Includes the silent-throttle trap: a big run reports `0 errors` while losing `duration`/`availability` on 79% of rows.** | ✅ 1,318 ingested, repaired and re-indexed - avatars live |
| [`05-performance`](05-performance/01-baseline.md) | Benchmark harness, measured baseline and per-route performance budgets for the API and the web app. **Budgets are enforced by `apps/web/tests/perf-budget.spec.ts`; waivers are ratchets that must be deleted once a route is fixed.** | ✅ Harness built, baseline captured 2026-08-09 |
| [`06-transcripts`](06-transcripts/02-architecture.md) | Free transcripts from YouTube's own `bg-orig` auto-captions, stored as windowed segments in a **second** Meilisearch index. **Includes the byte-vs-character typo-tolerance bug that made `пица` match `пичове` - 95 of 100 hits were false.** Probe page: `01-caption-probe.html`. | ✅ Built 2026-08-09 - storage, search and CLI live |
| [`07-visual-redesign`](07-visual-redesign/01-implementation.md) | The full visual redesign, built from `Designs/design_handoff_podcast_index/`. **Includes the WCAG failures in the handed-over palette and the density fallback the transposed mobile grid needs at 11 years.** | ✅ Built 2026-08-10 - all 10 routes, 746 tests green |
| [`08-demo-data`](08-demo-data/01-seeding-and-verification.md) | Reversible demo seeding (`seed_demo`) and the full-app verification pass on top of it. **The seed data caught a false "metadata complete" claim in our own docs - 1,076 rows were still degraded - and the write-up then mis-scoped a count of its own. Both errors are recorded.** | ✅ Seeded and verified 2026-08-10 - 753 tests green, 0 degraded rows |
| [`09-edge-case-hardening`](09-edge-case-hardening/01-findings.md) | Hostile-input sweep across every route, plus the data gaps that made `/me/tags` and the moderation queue look unbuilt. **Found the real cause of the "unexplained flaky E2E": `CONN_MAX_AGE=600` under `runserver` exhausted Postgres, and Next's stale fetch cache disguised the 500s as wrong data.** Also a NUL byte 500ing ten endpoints, `?q=???` returning the whole catalogue, and an 11th click on "Зареди още" serving a 500. | ✅ Swept 2026-08-11 - 7 bugs fixed, 958 tests green |
| [`10-deployment`](10-deployment/01-production-setup.md) | Production deployment: Vercel (web) + Railway (API, Celery, Postgres, Redis, Meilisearch), comedycommunity.club DNS, Clerk production instance. **Six gotchas recorded, incl. gunicorn never having been a dependency, a Meilisearch version drift that silently killed typo tolerance, and the Railway agent reporting success for config it never applied.** | ✅ LIVE 2026-08-15 - 13/13 launch smoke tests green |
| [`11-ux-feedback`](11-ux-feedback/01-backlog.md) | The owner's first full walkthrough of the live site, turned into 31 tracked tasks. **Includes the ruling that reverses the i18n deferral: English becomes the default UI locale, Bulgarian stays for content.** Records why cookie-driven i18n was chosen over an `app/[locale]/` rewrite. | 🚧 In progress |
| [`12-search-and-memberships`](12-search-and-memberships/01-search-counts-and-matching.md) | Search counts that lied (a heading counting one of two result sets, an episode total that was an artefact of a *segment* page size, and a section capped below the number it advertised), plus loose-but-labelled multi-word matching. Then self-managed memberships whose **month count is derived, never stored**, the profile-icon ladder, and the fullscreen channel grid. **Records the twice-repeated lesson that a hardcoded pixel constant standing in for a layout is a bug waiting for a different viewport.** | ✅ Built 2026-08-16 |
| [`13-ux-feedback-round-3`](13-ux-feedback-round-3/01-changes.md) | The owner's walkthrough one day after the search/memberships work: 14 items, two of them undoing something shipped hours earlier. **Records why machine-suggested topic labels must be derived from `added_by` rather than a new column - and why `added_by IS NULL` is a deleted member, never the machine.** Also the fullscreen channel grid being deleted one day old (the cell count was the problem, not the viewport) and a sticky column's box-shadow permanently clipping the first grid column. | ✅ Built 2026-08-16 |
| [`14-participants-and-moments`](14-participants-and-moments/01-design.md) | Community-proposed episode participants with admin verification, and the moment-logging form. **Records why user input must never create a `Person` row: the auto-captions mishear Тонката as Донката, and free-text personas would split one filmography across three pages.** Proposals live in their own table so no existing reader can leak pending data into search by omission. `02` adds user problem-reporting with a resolution-note feedback loop and admin activity visibility - **records that the existing bulk resolve action cannot write a note, which is the entire loop, and that `MomentAdmin` sorts by position-in-episode so 'what is being added right now' is unanswerable today.** | ✅ Built and deployed 2026-08-16 |
| [`15-ratings-grid-flow`](15-ratings-grid-flow/01-the-grid-that-wraps.md) | The ratings grid stops being a matrix and starts wrapping. **On a laptop the flagship channel's grid was 3,913px inside a 1,150px card, so 52 of 183 columns were visible and the site looked a quarter of its size.** Records why this is the THIRD attempt and the first two failed the same way (both treated "show me everything" as a scaling problem), why the two density decisions had to split apart once there was no width to run out of, and the 788 empty spacer cells the matrix was shipping purely to keep years aligned. | ✅ Built 2026-08-16 |
| [`16-navigation-feel`](16-navigation-feel/01-why-everything-felt-slow.md) | Why the whole site felt slow: reading the locale cookie made every route dynamic, and **Next skips prefetching dynamic routes entirely unless they have a `loading.tsx`** - of which this app had exactly one. Every click blocked on a full round trip with no indication. Records why the correct "never `app/loading.tsx`" rule was over-applied into "never any loading file", why `/channels` and `/me` needed route groups to get a boundary at all, and the `/search` overlay that closed itself two seconds before the results arrived. Also the moment timestamp that **could not be typed on a phone** (`inputMode="numeric"` has no colon key) and is now optional, and two new cast roles - which surfaced that `approve` never validated the role, so the unvalidated path was the privileged one. | ✅ Built 2026-08-16 |
| [`17-moderation-surfaces`](17-moderation-surfaces/01-what-the-second-walkthrough-found.md) | The walkthrough right after the navigation work. **A display name saved successfully and was overwritten by Clerk on the very next request** - `provision_user` refreshed "fields the identity provider owns" on every authenticated call, so the PATCH returned 200 with the new name and the next GET returned 200 with the old one. Also three endpoints built with no caller (the report queue, the reporter's feedback loop, and any record that a proposal was ever decided) - **an endpoint with no reader is not a feature** - plus why a report payload of "comment 41" cannot be acted on, why the proposal queue groups by episode AND proposer, and the `/api/people` `limit` that was a cap rather than pagination. Records a moment that appeared to vanish because a community read sat in the 60-second fetch cache. | ✅ Built 2026-08-16 |
| [`18-pre-launch-audit`](18-pre-launch-audit/01-findings.md) | Unattended pre-launch sweep of registration, authorization, the admin surfaces, every episode action and the production infrastructure - each finding verified by running something against local AND live production, not read off the code. **Auth holds under attack** (forged `alg:none`, dev-token and unauthenticated probes all 401 in prod) and the injection/XSS surface is clean. **But membership verification cannot work in production at all: four independent breaks in one flow** - no object storage is wired, the container disk is ephemeral, `MEDIA_URL` is unserved outside `DEBUG` (so the admin's screenshot preview 404s), and `is_verified` has exactly one writer in Django Admin - **which app-promoted admins cannot log into**, because a Clerk-provisioned account has no password. Also `/admin/` publicly reachable with no brute-force protection, no Sentry on either half, no `robots.txt`/`sitemap.xml` on a site whose stated value is discoverability, no ban path and an unused `CLERK_WEBHOOK_SECRET`, and a per-account write throttle that N sign-ups defeat. **`02-fixes.md` records what was then closed** (owner ruling: leave verification, fix the rest) - suspension reusing Django's `is_active` and checked in the AUTH BACKEND so no future endpoint can ship reachable by a banned account, an admin lockout keyed on **username not IP** (behind Railway's edge, `REMOTE_ADDR` is a proxy - so IP keying either locks out the whole internet on the first ten failures or is bypassed with a header), API Sentry, a paged sitemap, and four writes that were never validated. Also records that **Cloudflare is not actually in front of either host**, contrary to the stack table, and a shared-Redis flake between the test suite and the dev server. | ✅ Swept and mostly closed 2026-08-16 - 2,079 tests green |
| [`19-ios-safari-compatibility`](19-ios-safari-compatibility/01-findings.md) | The app's first pass on the engine iOS actually ships. **386 tests across two "mobile" viewports had never run one line on Safari** - both Playwright projects were Desktop Chrome, one of them resized to 390x844. Under real WebKit the app is sound (zero console errors, zero page errors, no sideways scroll at 320/375/390/768 or landscape), but **every form control in the product zoomed the viewport on focus and never zoomed back**: the rule preventing it existed in `@layer base` and was inert, because `.text-small` (components) and `text-[15px]` (utilities) outrank `base` no matter how the selector is written. Records why the fix has to be UNLAYERED, why `viewport-fit: cover` would create the safe-area bug it looks like it prevents, and why the 44px hit areas are grown with a pseudo-element whose collisions are invisible. The new spec was verified by disabling the fix and watching exactly four tests go red. | ✅ Swept and fixed 2026-08-18 - 404 E2E green |
| [`20-search-index-warmth`](20-search-index-warmth/01-the-index-that-went-cold.md) | Why a live search rendered "transcript search is unavailable" while everything was healthy. **Meilisearch answered the same request with `time.busy=5.13ms` and `time.idle=14.5s`** - it searched in five milliseconds and then took fourteen and a half seconds to say so, long after the API's 5s read timeout had given up. The small `episodes` index answered in 14.6ms in that same instant; the 61,452-segment transcript index had been paged out. Records why the health check cannot see this (**a healthy server and a resident index are different claims**), why raising the timeout was rejected, and the two ways a keep-warm task silently warms nothing - a stop-word query that matches nothing, and timing `processingTimeMs` instead of wall clock. Also the ratings grid's year blocks flipping to newest-first, and the renumbering bug that reversal invites. | ✅ Fixed 2026-08-18 |
